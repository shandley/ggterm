{
  "name": "ggterm",
  "version": "0.2.0",
  "private": true,
  "description": "A Grammar of Graphics for Terminal User Interfaces",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '@ggterm/core' build && bun run --filter '@ggterm/render-*' build && bun run --filter '!@ggterm/core' --filter '!@ggterm/render-*' build",
    "test": "bun test packages/core && bun test packages/solid && bun test packages/svelte && bun test packages/vue",
    "test:core": "bun test packages/core",
    "test:solid": "bun test packages/solid",
    "test:svelte": "bun test packages/svelte",
    "test:vue": "bun test packages/vue",
    "lint": "eslint packages/*/src/**/*.ts",
    "lint:fix": "eslint packages/*/src/**/*.ts --fix",
    "format": "prettier --write 'packages/*/src/**/*.ts'",
    "format:check": "prettier --check 'packages/*/src/**/*.ts'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.3.0",
    "eslint": "^8.56.0",
    "@typescript-eslint/eslint-plugin": "^6.19.0",
    "@typescript-eslint/parser": "^6.19.0",
    "prettier": "^3.2.0"
  },
  "author": "Scott Handley",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/handleylab/ggterm.git"
  },
  "homepage": "https://github.com/handleylab/ggterm#readme",
  "bugs": {
    "url": "https://github.com/handleylab/ggterm/issues"
  },
  "keywords": [
    "grammar-of-graphics",
    "terminal",
    "tui",
    "visualization",
    "charts",
    "plots",
    "braille",
    "unicode"
  ]
}
